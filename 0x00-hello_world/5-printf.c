#include <stdio.h>
/**
* main -> main code will print a string that is with
* proper grammar, but the outcome is a piece of art
*
* Return: Always 0 (success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
