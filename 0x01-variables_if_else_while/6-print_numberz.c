#include <stdio.h>
/**
 *  * main -> Write a program that prints the alphabet in lowercase, followed by a new line.
 *   *
 *    * RETURN -> always 0
 *     */
int main(void)
{
     int ch;
     for (ch= 0; ch<= 10; ch++)
	 {
		 putchar(ch);
		 putchar('\n');
	 }
		 return (0);
}

